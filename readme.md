Basic Anti-spam bot created for the ZIM Discord server as a temporary anti-spam solution.
Others exist (Wick, Automod, etc.), but those require special permissions to setup, as an admin, I couldn't set them up, so this was a simple drop in solution.

This bot will block all mention spam, including user mentions, so it's rather harsh.